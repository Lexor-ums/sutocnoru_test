<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RecensionButton" parent="Theme.MaterialComponents.Light">
        <item name="android:colorButtonNormal">@color/button_color</item>
    </style>
    <style name="RatingBar"  parent="android:style/Widget.Material.RatingBar">
        <!--<item name="android:progressDrawable">@drawable/custom_ratingbar</item>-->
        <item name="android:secondaryProgress">@color/mark_color</item>
        <item name="colorControlActivated">@color/mark_color</item>
    </style>
</resources>
